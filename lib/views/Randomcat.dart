/////VISTA 3
import 'package:flutter/material.dart';

class Randomcat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Michis 2')),
      body: Center(
        child: Text("Randoms cats's"),
      ),
    );
  }
}
